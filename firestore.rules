rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All database reads and writes are strictly orchestrated server-side
    // via firebase-admin SDK. Direct client access is denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
